import 'package:flutter/material.dart';

class ResellersScreen extends StatelessWidget {
  const ResellersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("ResellersScreen")));
  }
}
